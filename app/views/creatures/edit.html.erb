<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Creature</title>
</head>
<body>
  <br><br>
  <div class='container'>

    <p><span class='glyphicon glyphicon-chevron-left'></span>
       <%= link_to('Back to Creatures', creatures_path) %>
    </p>

    <div class='row'>
      <div class='col-md-6'>
        <div class='well'>

          <div class='row'>
            <div class='col-md-6 col-md-offset-1'>
              <h3>Edit Creature</h3>
            </div>
          </div>
          <br>

          <!-- Allowing form_for to infer action and method -->
          <!-- Interesting: Examining the form generated in Chrome Inspector,
          it appears that method="post" was inferred.  However we know that the
          patch route from routes.rb is being utilized.  Hmm. -->
          <%= form_for(@creature) do |f| %>
            <div class='row'>
              <div class='col-md-3 text-right'>
                <%= f.label "Name: " %>
              </div>
              <div class='col-md-8'>
                <%= f.text_field(:name, { autofocus: "true" }) %>
              </div>
            </div>
            <br>
            <div class='row'>
              <div class='col-md-3 text-right'>
                <%= f.label "Description: " %>
              </div>
              <div class='col-md-8'>
                <%= f.text_area(:description, { rows: 3 }) %>
              </div>
            </div>
            <br><br>
            <div class='row'>
              <div class='col-md-11 text-right'>
                <%= f.submit("Save changes", class: "btn btn-primary") %>
              </div>
            </div>
            <br>
          <% end %>

        </div>    <!-- well -->

      </div>
    </div>
  </div>
</body>
</html>